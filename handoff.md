# CinemaDNA 短剧AI数字制片厂 · 项目现状与交接说明（给 Claude）

**文档版本**：v1.1（2026-07-23 优化版）  
**用途**：让 Claude 快速、准确理解本项目当前真实状态、核心架构与开发起点。  
**重要声明**：本文档完全诚实。当前项目仍处于**纯概念与架构设计阶段**，**没有任何可运行代码、真实数据或端到端验证结果**。

---

## 1. 项目核心目标（必须完整理解）

用户（Lin Hui）的核心诉求是：

> 现有短剧画面场景布局和演员人物很不自然。希望先做从大数据自然天然的场景采集、收集、修改、拼接，像真实拍电影一样布局场景，呈现 3D 立体空间并可变化使用。把需要的场景收集保存编辑，然后让剧本去这个数据库调度这些场景，并按需拼接新场景。  
> 每个新剧把剧本给场景大模型，它会理解每个场景需要什么，先在大数据里找；如果没有，则根据剧本在网上大数据找到匹配的自然场景原子，然后布局好每场需要的场景。自然的多人脸生成新脸也要备份大数据，跟这个在一个大模型里。

最终愿景被总结为：

**一个能读懂剧本场景需求，从内部 SceneDNA 大数据中调用自然场景原子，缺失时自动从网上大数据抽象场景规律，组合出新的电影化空间，同时调用 IdentityDNA 生成自然新脸、年龄线和家族脸谱，并把所有成功场景和身份资产沉淀回统一资产大脑的短剧 AI 数字制片厂。**

### 关键新增原则（2026-07-22 ~ 07-23 迭代后）

1. **多故事并行连续生产**（最高优先级）  
   剧本模块完成后立即把任务交给下游，同时立刻可以启动下一部剧的剧本生产。不是等一部剧从剧本到视频全部做完才能开下一部。

2. **统一资产大脑拆成三大专用大模型**  
   - **SceneDNA 自然场景大模型**：搜索自然场景 → 提取原子 → 按剧本重新布局组合  
   - **IdentityDNA 自然多人脸合成大模型**：必须用多张自然人脸融合生成新脸（解决肖像权与版权），支持年龄线与家族脸谱，严禁单一真人高相似克隆  
   - **PropDNA 道具生成大模型**：按剧本生成自然道具并保持跨镜头状态连续  

3. 所有成功资产必须回流数据库，供后续新剧本直接调用。

4. 全流程通过网页操作，严格 Production Gate，Active Production Bundle 隔离，四元组（story_id + bundle_id + task_id + asset_hash）绑定。

---

## 2. 当前真实状态（诚实评估）

| 项目 | 状态 | 说明 |
|------|------|------|
| 可运行代码 | **无** | 没有任何项目目录、依赖安装或可执行脚本 |
| 真实数据 | **无** | SceneDNA / IdentityDNA / PropDNA 数据库全部为空 |
| 端到端验证 | **未做** | 从未用真实或假剧本跑通过完整链路 |
| 3D 重建 / 真实生成 | **未实现** | 仅停留在架构讨论 |
| 架构设计 | **已完成并迭代** | 已形成完整规格说明书 |

**核心结论**：  
目前只有高质量的架构文档，工程落地尚未开始。Claude 接手后必须从零搭建，不能假设任何代码已存在。

---

## 3. 已产出的核心规格文件（请优先阅读）

项目内已准备好以下三份核心文档，Claude 必须先完整阅读再写任何代码：

1. **CinemaDNA_DramaOS-X_Codex实现规格说明书_v1.1.md**  
   总规格：系统目标、五层架构、三大资产大模型、生产铁规、并行工厂原则。

2. **短剧AI数字制片厂_全子模型多智能体详细分工与成片协作流程_v1.0.md**  
   每一个子模型需要哪些多智能体、每个智能体的输入输出、以及完整成片协作时序。

3. **三大资产模型_第一阶段可执行接口与Workorder数据结构_v1.0.md**  
   Phase 1 可直接落地的接口定义、通用 Workorder 结构、Gate Report、Backflow Record、验收标准。

这三份文件是当前最权威的设计依据。任何实现都必须与之对齐。

---

## 4. 推荐的落地起点（Phase 1）

**不要一上来就做完整视频生成。** 按以下顺序推进：

1. **工厂骨架**  
   - Active Production Bundle 目录结构  
   - 四元组校验  
   - Pipeline Orchestrator 多故事状态机（支持「剧本提交后立刻启动新故事」）

2. **三大资产模型 Phase 1 接口**（先用 mock）  
   - 通用 Workorder 流转  
   - SceneDNA / IdentityDNA / PropDNA 的检索 → Workorder → Gate → Backflow 闭环  
   - IdentityDNA 必须实现「禁止单一真人克隆」的硬性 Gate 逻辑

3. **ScriptBrain 最小可运行版**  
   - 先能输出标准化的 shooting_script.json + scene_export

4. 后续再接 Director → Performance → Render（mock）→ QA → 回流

**Phase 1 验收标准**：  
用一份假 shooting_script.json 跑通「查库 → 缺失创建 Workorder → mock 生成 → Gate → 回流」完整闭环，且系统支持同时启动第二部剧本。

---

## 5. 绝对不能踩的坑

1. **版权与肖像权红线**  
   IdentityDNA 必须走多人脸融合，禁止单一真人高相似克隆。真实场景采集必须使用公开授权或合成数据。

2. **「真实场景原子优先」原则被稀释**  
   如果因为获取真实数据困难就退化成纯 AI 生图，核心差异化会丧失。

3. **资产污染**  
   没有质量门槛的自动回流会导致数据库迅速充满低质量资产。必须有 Gate。

4. **过早承诺生产级质量**  
   当前技术距离「像真实拍电影一样」仍有明显差距。先做到接口闭环与一致性可控，再逐步提升生成质量。

5. **忽略并行工厂核心**  
   如果实现成「一部剧全部做完才能开下一部」，就违背了用户最强调的生产模式。

---

## 6. 给 Claude 的执行建议

1. 先完整阅读上述三份核心规格说明书。
2. 确认理解「多故事并行 + 三大专用资产大模型 + 强制回流 + Production Gate」。
3. 从工厂骨架和三大资产模型的 Phase 1 接口开始写代码。
4. 所有输出使用结构化 JSON，并绑定四元组。
5. 保持 Phase 1 边界清晰：先 mock，再替换真实生成能力。
6. 每完成一个模块，更新状态说明。

---

**文档结束**

*本文件由原交接文档优化而来，已同步最新架构决策（三大资产大模型细分、并行工厂、Phase 1 接口定义），专门用于给 Claude 提供清晰、诚实的项目起点。*
